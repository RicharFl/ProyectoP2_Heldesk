import { FilterTicketPipe } from './filter-ticket.pipe';

describe('FilterTicketPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTicketPipe();
    expect(pipe).toBeTruthy();
  });
});
