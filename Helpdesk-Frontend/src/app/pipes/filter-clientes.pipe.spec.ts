import { FilterClientesPipe } from './filter-clientes.pipe';

describe('FilterClientesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClientesPipe();
    expect(pipe).toBeTruthy();
  });
});
