import { FilterServiciosPipe } from './filter-servicios.pipe';

describe('FilterServiciosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterServiciosPipe();
    expect(pipe).toBeTruthy();
  });
});
