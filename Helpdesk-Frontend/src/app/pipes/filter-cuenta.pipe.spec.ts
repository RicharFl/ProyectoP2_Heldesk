import { FilterCuentaPipe } from './filter-cuenta.pipe';

describe('FilterCuentaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCuentaPipe();
    expect(pipe).toBeTruthy();
  });
});
