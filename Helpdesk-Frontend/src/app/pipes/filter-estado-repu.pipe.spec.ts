import { FilterEstadoRepuPipe } from './filter-estado-repu.pipe';

describe('FilterEstadoRepuPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEstadoRepuPipe();
    expect(pipe).toBeTruthy();
  });
});
