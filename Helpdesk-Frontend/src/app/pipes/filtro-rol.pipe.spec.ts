import { FiltroRolPipe } from './filtro-rol.pipe';

describe('FiltroRolPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroRolPipe();
    expect(pipe).toBeTruthy();
  });
});
