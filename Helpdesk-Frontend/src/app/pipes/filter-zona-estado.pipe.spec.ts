import { FilterZonaEstadoPipe } from './filter-zona-estado.pipe';

describe('FilterZonaEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterZonaEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
